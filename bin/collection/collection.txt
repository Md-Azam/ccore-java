Problem1 -  Steps to find occurence of each character in a given string using hashmap .

	 /*
	   Define a Method with return type  Map<Char, Integer) and a String argument
	  
	 Create a map to store character occurrences
	 Iterate through each character in the input string  - for (char c : input.toCharArray())
	 Update the count in the map -  charCountMap.put(c, charCountMap.getOrDefault(c, 0) + 1)
	 
	 Call this method i main function .
	 Display the occurrences of each character using entryset.
	 */

